fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
jinja2>=3.1
numpy>=1.26
scipy>=1.11
trimesh>=4.0
matplotlib>=3.8

# Optional. Enables distance_backend="bvh", which answers the surface-distance
# question by hierarchy traversal instead of dense sampling: ~69x faster per
# evaluation on the reference part, agreeing to 9e-16 mm. Without it the app
# runs unchanged on the "sampled" backend and the UI hides the option.
python-fcl>=0.7
